import { defineConfig } from 'astro/config';

// Cambia `site` por tu dominio definitivo antes de publicar.
export default defineConfig({
  site: 'https://alejandraarochaovalles.github.io',
  build: { inlineStylesheets: 'auto' },
});
